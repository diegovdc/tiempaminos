* Visibility
#+begin_src lua
function onReceiveOSC(message, connections)
  local arg1 = message[2][1]
  if type(arg1) == "boolean" then
    self.visible = arg1
  end
end
#+end_src

* Color
#+begin_src lua
function onReceiveOSC(message, connections)
  local arguments = message[2]
  self.color = Color.fromHexString(arguments[1].value)
end
#+end_src
