(ns tieminos.habitat.parts.dia
  (:require [taoensso.timbre :as timbre]))

(defn dueto-con-polinizadores
  [inputs base-preouts]
  (doseq [[k {:keys [bus]}] inputs]
    (timbre/warn "Not implemented yet")))

(defn escucha-de-aves
  [inputs base-preouts]
  (doseq [[k {:keys [bus]}] inputs]
    (timbre/warn "Not implemented yet")))
